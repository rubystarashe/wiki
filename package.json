{
  "name": "wiki",
  "version": "1.0.0",
  "description": "RubystarAshe&#39;s web dev wiki",
  "author": "RubystarAshe",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "publish": "npm run generate && npm run deploy"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.0.0",
    "@nuxtjs/markdownit": "^1.2.2",
    "cross-env": "^5.2.0",
    "nuxt": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
